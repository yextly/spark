---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: workerinstances.compute.yextly.io
spec:
  group: compute.yextly.io
  names:
    kind: WorkerInstance
    listKind: WorkerInstanceList
    plural: workerinstances
    singular: workerinstance
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkerInstance is the Schema for the workerinstances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkerInstanceSpec defines the desired state of WorkerInstance.
            properties:
              templateName:
                description: Specifies the name of the template to use
                type: string
              workerId:
                description: Specifies the unique identifier of the worker that will
                  be used for Job scheduling
                type: string
            required:
            - templateName
            - workerId
            type: object
          status:
            description: WorkerInstanceStatus defines the observed state of WorkerInstance.
            properties:
              jobName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
