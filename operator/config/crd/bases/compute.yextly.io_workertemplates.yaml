---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: workertemplates.compute.yextly.io
spec:
  group: compute.yextly.io
  names:
    kind: WorkerTemplate
    listKind: WorkerTemplateList
    plural: workertemplates
    singular: workertemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkerTemplate is the Schema for the workertemplates API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkerTemplateSpec defines the desired state of WorkerTemplate.
            properties:
              jobTemplate:
                description: Specification of the desired behavior of the pod in the
                  form of a PodSpec.
                type: object
                x-kubernetes-preserve-unknown-fields: true
            required:
            - jobTemplate
            type: object
          status:
            description: WorkerTemplateStatus defines the observed state of WorkerTemplate.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
