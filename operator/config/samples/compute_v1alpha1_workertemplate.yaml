apiVersion: compute.yextly.io/v1alpha1
kind: WorkerTemplate
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: workertemplate-sample
spec:
  # TODO(user): Add fields here
